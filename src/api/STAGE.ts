import type { Walker } from "../models/Walker.ts"

/**
 * @deprecated Use `STAGE` from 'isomorphic-git/commands' instead
 * 
 * Get a git index Walker
 *
 * See [walk](./walk.md)
 *
 * @returns {Walker} Returns a git index `Walker`
 *
 */
export { STAGE } from '../commands/STAGE.ts'

